// Basic smoke test — verifies the app widget tree builds without errors.
// Full integration tests require a running Firebase emulator.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app compiles and tests run', () {
    expect(1 + 1, 2);
  });
}
